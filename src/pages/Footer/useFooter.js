import useQuery from '@/hooks/useQuery';
import newService from '@/service/newService';
import React from 'react';

const useFooter = () => {
  const footerTopProps = {
    dataCategories,
  };
  return { footerTopProps };
};

export default useFooter;
